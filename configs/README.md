# Experiment manifests

This directory will hold versioned experiment manifests. A manifest must declare
environment family, topology and parameter split, policy/baseline matrix,
sensing budget, seeds, metrics, and whether it is development or locked
confirmation evidence.

No result-derived policy selection belongs in a confirmation manifest.

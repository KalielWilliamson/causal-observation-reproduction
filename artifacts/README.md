# Released artifacts

Raw run outputs are ignored by default. This directory tracks only release
manifests, checksums, compact result summaries, and instructions for obtaining
or regenerating released evidence.
